package com.majq.schat.constant;

public interface FrameConstant {
    /**
     * 主窗口标题
     */
    String TITLE = "SChat";
    /**
     * 默认主窗口宽度
     */
    int DEFAULT_WIDTH = 900;
    /**
     * 默认主窗口高度
     */
    int DEFAULT_HEIGHT = 600;
    /**
     * 主窗口默认显示位置-X  即窗口左上角距屏幕左上角x方向距离
     */
    int DEFAULT_LOCATION_X = 900;
    /**
     * 主窗口默认显示位置-Y  即窗口左上角距屏幕左上角y方向距离
     */
    int DEFAULT_LOCATION_Y = 200;
}
